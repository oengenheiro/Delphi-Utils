This sample implementes ITetheringObserver and ITetheringSubject

It uses a Memo to output when the application is connected/disconnected from another

To test, run two instances of the application and click auto-connect button on one

It should output the TTetheringManaferInfo record on the Memo

If you close one of them, it should output for a disconnect